# Core (agent, models, tests)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
langgraph>=0.2
langchain-core>=0.3
requests>=2.31
beautifulsoup4>=4.12
pytest>=7.4
pytest-cov>=4.1

# Explainability (notebooks/week3_final_agent.ipynb, Section 10)
shap>=0.44

# Bonus interface layer (app/)
streamlit>=1.30
plotly>=5.18

# Optional: only needed if you enable the cloud planner (opt-in, see docs)
# anthropic>=0.30

# Sandbox target app (sandbox/target_app.py) -- for live recon demo
flask>=3.0
